3단계: 꼬였을 때 푸는 법 (Reset)
만약 1, 2단계를 했는데도 뭔가 꼬여서 계속 에러가 난다면, 기존 연결 고리를 끊고 다시 연결하는 게 빠릅니다. (파일은 안 지워집니다)

터미널에서 순서대로 치세요.

기존 연결 삭제:

Bash
git remote remove origin
주소 다시 연결 (주소는 본인 걸로 정확히!):

Bash
git remote add origin https://github.sec.samsung.net/아이디/프로젝트명.git
다시 푸시:

Bash
git push -u origin main
요약
VS Code 터미널을 연다.

git push origin main을 쳐본다.

**"Repository not found"**면 -> 웹사이트 가서 방부터 만드세요. (제일 확률 높음)

로그인 실패면 -> 토큰 다시 확인하세요.

이 과정을 거쳐서 터미널에서 성공하면, 웹앱에서도 무조건 됩니다. 터미널에서 무슨 에러가 뜨는지 확인해 보세요.