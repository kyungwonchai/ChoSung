심 팁: 윈도우에서 git push가 실패하는 주원인은 **"인증(Credential)"**입니다. 웹앱이 백그라운드에서 실행될 때, Git이 로그인 정보를 물어보는 팝업을 띄우려다 멈추는 경우가 많습니다. 이럴 때는 터미널에 아래 명령어를 미리 입력해 두세요: git config --global credential.helper manager (이미 되어있다면 상관없습니다.)

혹시 깃허브 푸시 시 발생하는 구체적인 에러 메시