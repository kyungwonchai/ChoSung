# -k: SSL 인증서 검증 무시 (Insecure)
# -x: 프록시 지정
curl -fsSL -k -x "$PROXY_URL" https://get.docker.com -o get-docker.sh

# =================================================================
# 5. 실행 (sudo -E 옵션으로 위에서 설정한 프록시 변수를 유지한 채 실행)
# =================================================================
sudo -E sh get-docker.sh