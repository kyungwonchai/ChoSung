# 1. CPU 성능 모드 설정
    run("sudo apt update && sudo apt install -y cpufrequtils")
    run("echo 'GOVERNOR=\"performance\"' | sudo tee /etc/default/cpufrequtils")
    run("sudo systemctl restart cpufrequtils")