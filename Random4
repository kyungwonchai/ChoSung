3.  **EXECUTE & LOG**:
    - Execute the command.
    - **Overwrite** the output to a file named `last_result.log`. (Do NOT append).
    - Syntax: `command > last_result.log 2>&1` 
    - (The `>` symbol ensures the file is cleared and rewritten every time).