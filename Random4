와야 할 수도 있습니다.

GPG 키 추가 (인증서 등록이 완벽하다면 아래 명령어가 막힘없이 돌아갑니다):

Bash
curl -fsSL -x http://10.244.254.254:8080 https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
저장소 설정:

Bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
4단계: 도커 패키지 클린 설치
이제 apt update 시 에러가 나지 않아야 합니다.

Bash
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
5단계: 도커 서비스(Daemon) 프록시 설정 (필수)
도커가 깔렸어도 docker pull을 하려면 도커 서비스 자체에도 프록시를 알려줘야 합니다.

디렉토리 생성:

Bash
sudo mkdir -p /etc/systemd/system/docker.service.d
설정 파일 작성:

Bash
sudo nano /etc/systemd/system/docker.service.d/http-proxy.conf
아래 내용을 복사해서 넣으세요.

Ini, TOML
[Service]
Environment="HTTP_PROXY=http://10.244.254.254:8080"
Environment="HTTPS_PROXY=http://10.244.254.254:8080"
Environment="NO_PROXY=localhost,127.0.0.1,*.samsung.com"
서비스 재시작:

Bash
sudo systemctl daemon-reload
sudo systemctl restart docker
6단계: 사용자 권한 부여 (sudo 없이 사용)
Bash
sudo usermod -aG docker $USER
🚨 중요: 이 명령 후 로그아웃 했다가 다시 로그인해야 docker 명령어를 바로 쓸 수 있습니다.

요약: 설치 후 관리 명령어
설치가 끝났다면 아래 명령어로 상태를 확인해 보세요.

실행 확인: docker ps

이미지 다운로드 테스트: docker pull hello-world

컴포즈 확인: docker compose version